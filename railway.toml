[build]
builder = "NIXPACKS"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar /usr/local/tomcat/bin/catalina.sh run"
healthcheckPath = "/StudentManagementSystem/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
